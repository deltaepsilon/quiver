{
  "private": true,
  "name": "@quiver/firebase-authentication",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./src/firebase-authentication.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch --template template.html",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "babel-core": "^6.26.0",
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "node-sass": "^4.7.2",
    "preact-cli": "^2.0.0",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "email-validator": "^1.1.1",
    "linkstate": "^1.1.0",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-material-components": "^1.2.4"
  }
}
